# Disabled in Actuna Mail (compliance/v0.1).
#
# Original purpose: build Linux x86_64 .deb / .rpm / .snap, code-sign,
# upload to AWS S3 distribution bucket. See header of build-linux-arm64.yaml
# for the full rationale. Re-enable only with a matching COMPLIANCE.md entry.

name: build-linux-disabled

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See header comment."
