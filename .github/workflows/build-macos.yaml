# Disabled in Actuna Mail (compliance/v0.1).
#
# Original purpose: build macOS .dmg for arm64 + x64, codesign with Apple
# Developer ID, notarize via Apple notary service, upload to S3.
# See header of build-linux-arm64.yaml for the full rationale.
# Apple codesign and notarization secrets are not inherited; workflow
# would fail at the codesign step. Re-enable only with a matching
# COMPLIANCE.md entry and after Actuna's own Apple Developer ID is set up.

name: build-macos-disabled

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See header comment."
