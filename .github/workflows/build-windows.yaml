# Disabled in Actuna Mail (compliance/v0.1).
#
# Original purpose: build Windows installer (Squirrel.Windows .nupkg + .exe),
# code-sign with Authenticode certificate, upload to AWS S3 + auto-update
# feed. See header of build-linux-arm64.yaml for the full rationale.
# Windows codesign cert (WIN_CSC_LINK) is not inherited; workflow would
# fail. Re-enable only with a matching COMPLIANCE.md entry and after
# Actuna's own Authenticode certificate is set up.

name: build-windows-disabled

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See header comment."
